#include "request.h"

#define REQUEST_IMPLEMENT(LOWER, UPPER, _) \
	SERIALIZE__IMPLEMENT(request_##LOWER, REQUEST_##UPPER);

REQUEST__TYPES(REQUEST_IMPLEMENT, );

SERIALIZE__IMPLEMENT(requestConnect, REQUEST_CONNECT);
